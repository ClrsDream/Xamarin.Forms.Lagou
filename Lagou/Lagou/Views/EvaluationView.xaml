<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Lagou.Views.EvaluationView">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <Label Text="{Binding Data.UserName}" TextColor="#999999" />
    <Label Text="{Binding Data.CreateTime, StringFormat='发表于:{0}'}" TextColor="#999999" Grid.Column="1" />

    <Label Text="{Binding Data.Content}"  TextColor="#999999" Grid.Row="1" Grid.ColumnSpan="2" />
    <Label Text="{Binding Data.OtherEvaluation}"  TextColor="#999999" Grid.Row="2" Grid.ColumnSpan="2" />
    
  </Grid>
  
</ContentView>